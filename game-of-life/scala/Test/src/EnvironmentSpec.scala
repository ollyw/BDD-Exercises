package gameoflife.tests

import org.scalatest._
import org.scalatest.matchers.ShouldMatchers

class Environment

class EnvironmentSpec extends FunSuite with ShouldMatchers {

	/*describe("A Stack") {

	    it("should pop values in last-in-first-out order") {
	      //val stack = new Stack[Int]
	      //stack.push(1)
	      //stack.push(2)
	      //assert(stack.pop() === 2)
	      //assert(stack.pop() === 1)
	    }
	/*"An Environment" should "be constructable" in {
	  //val environment = Environment()
	 //environment.getClass().toString() should startWith ("Environment")
	}*/
	}*/
  
  //change
}